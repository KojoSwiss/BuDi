<div class="row justify-content-center mt-4">
  <div class="col-md-10">
    <%= form_tag tasks_path, method: :get do %>
    <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Search for the service you want"
  %>
  <%= submit_tag "Search", class: "btn btn-primary mb-2 mt-2" %>
<% end %>
  </div>
</div>

<div class="row justify-content-center">
  <div class="col-md-5">
    <% num = 0 %>
    <% @tasks.each do |task| %>
      <% num += 1 %>
      <h2><%= num %> | <%= link_to task.title.capitalize, task_path(task) %> | <%= link_to task.get_upvotes.size, like_task_path(task), method: :put, remote: :true, class:'btn btn-default'%> Likes</h2>
      <p> <em>Company name: </em> <%= task.company %></p>
      <p> <em> Location: </em> <%= task.location %></p>
      <hr>
    <% end %>
  </div>

  <div class="col-md-5">
    <div class="ontop">
      <div class="jumbotron">
        <h1 class="display-4">Join Now for FREEEEE!!!!, </h1>
        <p class="lead">Add your service here 😉, and let your customer find you easily</p>
        <hr class="my-4">
        <p><%= link_to 'New Service', new_task_path, class:"btn btn-primary btn-lg"  %></p>
      </div>
    </div>
  </div>
</div>

